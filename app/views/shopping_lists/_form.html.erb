<!-- _form.html.erb -->

<%= form_for(@shopping_list) do |f| %>
  <% if @shopping_list.errors.any? %>
    <div data-alert class="alert-box" id="error_explanation">
      <h2><%= pluralize(@shopping_list.errors.count, "error") %> prohibited this shopping_list from being saved:</h2>

      <ul>
      <% @shopping_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
      <a href="#" class="close">&times;</a>
    </div>
  <% end %>

  <div class="field row">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field row">
    <%= collection_check_boxes(:shopping_list, :grocery_ids, @groceries, :id, :name) %>
  </div>
  <div class="actions row">
    <%= f.submit "Save", class: "button" %>
  </div>
<% end %>
